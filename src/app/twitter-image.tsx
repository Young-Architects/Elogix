/**
 * twitter-image — reuses the Open Graph card so X/Twitter shows the same
 * large logo preview. Presence of this file also makes Next.js set
 * `twitter:card` to `summary_large_image` automatically.
 */
export { default, alt, size, contentType } from "./opengraph-image";
